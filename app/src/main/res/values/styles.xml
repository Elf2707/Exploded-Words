<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="PreferencesTheme">
        <item name="android:windowBackground">@drawable/main_act_gradient</item>
        <item name="android:textColor">@color/clr_main_lbl</item>
    </style>
</resources>
